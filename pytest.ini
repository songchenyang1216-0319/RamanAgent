[pytest]
testpaths = tests
pythonpath = .
addopts = -q -p no:cacheprovider
markers =
    integration: tests that require a running service or external dependency
    slow: tests that may take more than a few seconds
